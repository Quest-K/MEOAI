<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meogo AI 전략 회의실</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            background: linear-gradient(135deg, #fdfcfb 0%, #e2d1c3 100%);
            height: 100-vh;
            overflow: hidden;
        }

        /* 수채화 느낌의 부드러운 배경 효과 */
        .watercolor-bg {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .chat-container::-webkit-scrollbar {
            width: 6px;
        }

        .chat-container::-webkit-scrollbar-thumb {
            background-color: rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .officer-card {
            transition: all 0.3s ease;
        }

        .officer-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        }

        /* 임원별 고유 수채화 컬러 */
        .bg-ceo { background-color: #e3f2fd; }
        .bg-cso { background-color: #f3e5f5; }
        .bg-cfo { background-color: #fff3e0; }
        .bg-cdo { background-color: #e8f5e9; }
        .bg-cmo { background-color: #fce4ec; }
        .bg-coo { background-color: #efebe9; }
        .bg-cpo { background-color: #e0f2f1; }
        .bg-cro { background-color: #fffde7; }
    </style>
</head>
<body class="flex h-screen p-4 gap-4">

    <!-- 좌측 사이드바: 임원진 리스트 -->
    <aside class="w-72 watercolor-bg rounded-3xl p-6 flex flex-col gap-4 shadow-sm overflow-y-auto">
        <div class="mb-4">
            <h1 class="text-2xl font-bold text-gray-800">Meogo</h1>
            <p class="text-xs text-gray-500">C-Level Executive Board</p>
        </div>
        
        <div id="officer-list" class="flex flex-col gap-3">
            <!-- 임원 카드들이 여기에 생성됩니다 -->
        </div>
    </aside>

    <!-- 중앙: 메인 회의장 -->
    <main class="flex-1 flex flex-col watercolor-bg rounded-3xl shadow-sm overflow-hidden">
        <!-- 헤더 -->
        <header class="p-6 border-b border-gray-100 flex justify-between items-center bg-white/50">
            <div>
                <h2 class="text-lg font-bold text-gray-700">전략 회의실</h2>
                <p class="text-xs text-gray-400">주제: Meogo 서비스 초기 안착 및 수익화 전략</p>
            </div>
            <div class="flex gap-2">
                <span class="px-3 py-1 bg-green-100 text-green-600 text-xs rounded-full font-medium">8인 접속 중</span>
            </div>
        </header>

        <!-- 채팅 영역 -->
        <div id="chat-container" class="flex-1 overflow-y-auto p-6 flex flex-col gap-4 chat-container">
            <!-- AI 메시지 예시 -->
            <div class="flex flex-col items-start max-w-[80%]">
                <div class="text-[10px] font-bold text-gray-400 ml-1 mb-1">CEO (비전 & 상생)</div>
                <div class="bg-white p-4 rounded-2xl rounded-tl-none shadow-sm text-sm text-gray-700 leading-relaxed">
                    반갑습니다 임원 여러분. Meogo 프로젝트가 이제 본격적인 궤도에 오르려 합니다. 외국인 관광객들에게 한국의 진정한 매력을 알리되, 소상공인과 상생하는 모델을 어떻게 유지할지 의견 부탁드립니다.
                </div>
            </div>
        </div>

        <!-- 입력창 -->
        <footer class="p-6 bg-white/50 border-t border-gray-100">
            <div class="relative">
                <input type="text" id="user-input" placeholder="대표님, 안건을 입력해 주세요..." 
                    class="w-full p-4 pr-12 rounded-2xl border-none focus:ring-2 focus:ring-blue-200 outline-none shadow-inner bg-gray-50/50 text-sm">
                <button onclick="sendMessage()" class="absolute right-3 top-1/2 -translate-y-1/2 p-2 bg-blue-500 text-white rounded-xl hover:bg-blue-600 transition-colors">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path d="M10.894 2.553a1 1 0 00-1.788 0l-7 14a1 1 0 001.169 1.409l5-1.429A1 1 0 009 15.571V11a1 1 0 112 0v4.571a1 1 0 00.725.962l5 1.428a1 1 0 001.17-1.408l-7-14z" />
                    </svg>
                </button>
            </div>
        </footer>
    </main>

    <!-- 우측: 분석 패널 -->
    <aside class="w-80 flex flex-col gap-4">
        <section class="watercolor-bg rounded-3xl p-6 shadow-sm flex-1">
            <h3 class="text-sm font-bold text-gray-800 mb-4 border-b pb-2">실시간 전략 분석</h3>
            <div class="flex flex-col gap-4">
                <div>
                    <span class="text-[10px] font-bold text-blue-500 uppercase">SWOT: Strength</span>
                    <p class="text-xs text-gray-600 mt-1">350개의 방대한 로컬 콘텐츠와 외국인 특화 K-라이프 미션의 독점성.</p>
                </div>
                <div>
                    <span class="text-[10px] font-bold text-red-500 uppercase">SWOT: Weakness</span>
                    <p class="text-xs text-gray-600 mt-1">1인 개발 및 운영으로 인한 8개 지역 현장 관리 리소스 부족.</p>
                </div>
                <div class="mt-4 pt-4 border-t border-dashed">
                    <span class="text-[10px] font-bold text-purple-500 uppercase">Target Audience</span>
                    <p class="text-xs text-gray-600 mt-1">한국의 대중문화를 깊이 있게 체험하고 싶은 2030 외국인 자유 여행자.</p>
                </div>
            </div>
        </section>

        <section class="bg-white/80 rounded-3xl p-6 shadow-sm">
            <h3 class="text-sm font-bold text-gray-800 mb-2">오늘의 의사결정</h3>
            <p class="text-[11px] text-gray-500 leading-relaxed">회의 내용을 바탕으로 도출된 핵심 실행 지침이 여기에 기록됩니다.</p>
        </section>
    </aside>

    <script>
        const roles = [
            { key: "CEO", name: "비전 & 상생", color: "bg-ceo", desc: "회사의 방향성과 비전을 결정" },
            { key: "CSO", name: "전략 & 경쟁", color: "bg-cso", desc: "시장 분석과 경쟁 전략 수립" },
            { key: "CFO", name: "재무 & 리스크", color: "bg-cfo", desc: "비용 수익성 및 리스크 관리" },
            { key: "CDO", name: "데이터 & 의사결정", color: "bg-cdo", desc: "데이터 기반 사용자 행동 분석" },
            { key: "CMO", name: "고객 & 마케팅", color: "bg-cmo", desc: "브랜드 인지도 및 고객 확보" },
            { key: "COO", name: "운영 & 효율", color: "bg-coo", desc: "프로세스 효율 및 실행 관리" },
            { key: "CPO", name: "제품 & UX", color: "bg-cpo", desc: "미션 품질 및 사용자 경험" },
            { key: "CRO", name: "매출 & 성장", color: "bg-cro", desc: "영업 전략 및 매출 파이프라인" }
        ];

        function init() {
            const listContainer = document.getElementById('officer-list');
            roles.forEach(role => {
                const card = document.createElement('div');
                card.className = `officer-card ${role.color} p-4 rounded-2xl flex flex-col gap-1 cursor-pointer`;
                card.innerHTML = `
                    <div class="flex justify-between items-center">
                        <span class="text-xs font-bold text-gray-800">${role.key}</span>
                        <div class="w-2 h-2 rounded-full bg-green-400"></div>
                    </div>
                    <span class="text-[10px] text-gray-600">${role.name}</span>
                `;
                listContainer.appendChild(card);
            });
        }

        function sendMessage() {
            const input = document.getElementById('user-input');
            const container = document.getElementById('chat-container');
            
            if (!input.value.trim()) return;

            // 사용자 메시지 추가
            const userMsg = document.createElement('div');
            userMsg.className = "flex flex-col items-end max-w-[80%] ml-auto";
            userMsg.innerHTML = `
                <div class="text-[10px] font-bold text-gray-400 mr-1 mb-1">대표님 (Owner)</div>
                <div class="bg-blue-500 text-white p-4 rounded-2xl rounded-tr-none shadow-sm text-sm leading-relaxed">
                    ${input.value}
                </div>
            `;
            container.appendChild(userMsg);
            
            // 자동 스크롤
            container.scrollTop = container.scrollHeight;
            input.value = '';

            // 가짜 AI 응답 시뮬레이션 (이후 실제 API 연동)
            setTimeout(() => {
                const aiMsg = document.createElement('div');
                aiMsg.className = "flex flex-col items-start max-w-[80%]";
                aiMsg.innerHTML = `
                    <div class="text-[10px] font-bold text-gray-400 ml-1 mb-1">CFO (재무 & 리스크)</div>
                    <div class="bg-white p-4 rounded-2xl rounded-tl-none shadow-sm text-sm text-gray-700 leading-relaxed border-l-4 border-orange-200">
                        대표님, 제안하신 방향은 좋으나 1인 개발 환경에서 350개 미션을 무료로 유지할 때 발생하는 서버 비용과 운영 리소스를 무시할 수 없습니다. 광고 수익 모델이 안착되기 전까지의 캐시플로우(Cash Flow) 확보 방안이 필요해 보입니다.
                    </div>
                `;
                container.appendChild(aiMsg);
                container.scrollTop = container.scrollHeight;
            }, 1000);
        }

        // 엔터키 입력 지원
        document.getElementById('user-input').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') sendMessage();
        });

        init();
    </script>
</body>
</html>
